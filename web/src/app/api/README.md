# Generated API Client

Do not edit files under `src/app/api/generated` manually. Regenerate them from the backend
OpenAPI specification with `npm run generate:api`.

The generated types and services represent the API boundary only. Map them into frontend
domain or view models when UI state needs a different shape.
